/*
 * Given this code snippet, draw the graph representation and upload a picture
 * of your graph.
 * NB: It is a weighted graph
 * // Correct graph
 */

// TODO Draw this image
/*
 *
 *
 * Map<Integer, Map<Integer, Integer>> graph = new HashMap<>();
 *
 * // Add nodes and edges with weights
 * graph.put(1, new HashMap<>(Map.of(2, 4, 3, 2)));
 * graph.put(2, new HashMap<>(Map.of(4, 5, 5, 1)));
 * graph.put(3, new HashMap<>(Map.of(7, 3)));
 * graph.put(4, new HashMap<>());
 * graph.put(5, new HashMap<>());
 * graph.put(6, new HashMap<>());
 * graph.put(7, new HashMap<>(Map.of(8, 2, 9, 4)));
 * graph.put(8, new HashMap<>());
 * graph.put(9, new HashMap<>(Map.of(4, 5, 5, 1)));
 */
